query FindUserRepo($user:String!, $name:String!) {
  user(login: $user) {
    repository(name: $name) {
      isPrivate
      defaultBranchRef {
        target {
					__typename
          oid
        }
      }
    }
  }
}